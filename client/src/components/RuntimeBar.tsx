export interface RuntimeState { sessionId?: string; sessionName?: string; thinkingLevel?: string; autoCompactionEnabled?: boolean; autoRetryEnabled?: boolean; steeringMode?: "all" | "one-at-a-time"; followUpMode?: "all" | "one-at-a-time"; isStreaming?: boolean; isCompacting?: boolean; pendingMessageCount?: number; messageCount?: number; cwd?: string; }
interface Props { state: RuntimeState | null; thinkingLevels: string[]; disabled: boolean; model?: string; connected: boolean; onThinkingChange: (level: string) => void; onAutoCompactionChange: (enabled: boolean) => void; onCompact: () => void; }
const projectName = (cwd?: string) => cwd?.replace(/\\/g, "/").split("/").filter(Boolean).slice(-2).join("/") || "No project";
export function RuntimeBar({ state, thinkingLevels, disabled, model, connected, onThinkingChange, onAutoCompactionChange, onCompact }: Props) { const level = state?.thinkingLevel || "off"; const activity = state?.isCompacting ? "Compacting" : state?.isStreaming ? "Generating" : connected ? "Ready" : "Offline"; return <div className="runtime-bar"><div className="runtime-track"><span className={`runtime-dot ${state?.isStreaming ? "live" : ""}`} /><span title={state?.cwd}>{projectName(state?.cwd)}</span><span className="runtime-divider" /><span>{model || "No model"}</span><span className="runtime-divider" /><strong>{activity}</strong></div><div className="runtime-controls"><label>Thinking<select value={level} onChange={(event) => onThinkingChange(event.target.value)} disabled={disabled || !thinkingLevels.length}>{(thinkingLevels.length ? thinkingLevels : [level]).map((item) => <option key={item}>{item}</option>)}</select></label><label className="runtime-toggle"><input type="checkbox" checked={Boolean(state?.autoCompactionEnabled)} onChange={(event) => onAutoCompactionChange(event.target.checked)} disabled={disabled} /><span>Auto compact</span></label><button type="button" onClick={onCompact} disabled={disabled}>Compact</button></div></div>; }
